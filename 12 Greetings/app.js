"use strict";
let names = ['Rayyan', 'Ali', 'Sheikh', 'Usman'];
let message = 'Your are invited to my event';
for (let i = 0; i < names.length; i++) {
    console.log(`Hey ${names[i]}, ${message}`);
}
