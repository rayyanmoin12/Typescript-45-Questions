// inside parenthese are two paramenters: size & message
function make_shirt(size:string, message:string){
    console.log(`The size of the T-shirt is ${size} and the message printed is "${message}".`);
}

// inside parenthesis are two arguements: Large & Work hard
// Call the function
make_shirt('Large', 'Work hard!');